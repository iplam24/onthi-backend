# Datasource configuration
spring.datasource.url=jdbc:mysql://165.22.250.40:3306/vedu_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT configuration
app.jwt-secret=YourSuperSecretKeyThatIsAtLeast256BitsLongAndShouldBeStoredSecurelyInProduction
app.jwt-expiration-milliseconds=86400000

# Server port
server.port=8080

# Application metadata
spring.application.name=V-Edu
app.version=1.0.0
app.description=V-Edu Spring Boot Backend
app.environment=development

# File upload
app.file-upload-dir=uploads
app.file-upload-base-url=/uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
