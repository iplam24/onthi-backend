name: Deploy VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /root/onthi-backend

            git fetch origin
            git reset --hard origin/main

            mvn clean package -DskipTests

            pkill -f 'v-edu-0.0.1-SNAPSHOT.jar' || true

            sleep 2

            nohup java \
              -Xms256m \
              -Xmx1024m \
              -jar target/v-edu-0.0.1-SNAPSHOT.jar \
              > app.log 2>&1 < /dev/null &

            exit 0